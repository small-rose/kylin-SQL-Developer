package com.kylin.plsql.core.format.plsql.model;

public enum PlSqlBlockType {
    PACKAGE_SPEC,
    PACKAGE_BODY,
    TYPE_SPEC,
    TYPE_BODY,
    PROCEDURE,
    FUNCTION,
    TRIGGER,
    ANON_BLOCK,
    IF,
    LOOP,
    FOR_LOOP,
    WHILE_LOOP,
    REPEAT_LOOP,
    CASE_BLOCK
}
